# Operational Playbook

*Auto-generated from graduated lessons. Do not edit manually.*

## Core Principles

1. **Be Portable**: All context must reside within `.agent/`. Never hardcode absolute paths.
2. **Be Explicit**: Document assumptions in spec/. Run `/recall` before every task.
3. **Be Safe**: Every high-impact action must pass `/verify` before `/checkpoint`.
4. **Be Traceable**: Wrap all shell commands with `tracer.py` to catch silent killers.
5. **Be Iterative**: `/dream` → `/graduate` is the learning loop. Run it nightly.

## Graduated Lessons

*No lessons graduated yet. Run `/dream` then `/graduate` after your first task.*
